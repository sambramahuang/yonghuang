DATABASE_URL=postgresql://localhost:5432/yonghuang
# Tests require a separate empty database; never point this at real data.
TEST_DATABASE_URL=postgresql://localhost:5432/yonghuang_test
PORT=3001
HOST=127.0.0.1
CORS_ORIGIN=http://localhost:5173
# Set a random secret of at least 32 characters. Tokens are signed and expire.
AUTH_SECRET=
# live | fixture. Fixture mode only recognizes the committed demo sentences.
EXTRACTION_MODE=fixture
OPENAI_API_KEY=
OPENAI_MODEL=
